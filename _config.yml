# Site Build Settings

# jekyll-paginate configuration
paginate:            5

# Hide future posts
future: false

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

sass:
  sass_dir: assets/css
  style: :nested

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           "#microcleanup"
description:	       "Do a #microcleanup today!"
author:              "https://microcleanup.com"
